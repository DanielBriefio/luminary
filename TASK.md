# Task

No active task. Phase 15 (unified posts schema + frontend) shipped.
See `PRODUCT_STATE.md` for current state and recent migrations.

When the next task starts, replace this file with the plan
(context, decisions, scope, what-not-to-change, phasing).
